// Validation can be added using Joi or express-validator if needed
// Example (using Joi):
// const Joi = require('joi');
// const uploadImageSchema = Joi.object({
//   title: Joi.string().required(),
//   visibility: Joi.string().valid('Y', 'N').required()
// });
// module.exports = { uploadImageSchema };
